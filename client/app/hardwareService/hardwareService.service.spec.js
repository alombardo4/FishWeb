'use strict';

describe('Service: hardwareService', function () {

  // load the service's module
  beforeEach(module('fishWebApp'));

  // instantiate service
  var hardwareService;
  beforeEach(inject(function (_hardwareService_) {
    hardwareService = _hardwareService_;
  }));

  it('should do something', function () {
  });

});
