'use strict';

describe('Service: feedingService', function () {

  // load the service's module
  beforeEach(module('fishWebApp'));

  // instantiate service
  var feedingService;
  beforeEach(inject(function (_feedingService_) {
    feedingService = _feedingService_;
  }));

  it('should do something', function () {
  });

});
