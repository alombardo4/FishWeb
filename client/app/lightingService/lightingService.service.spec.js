'use strict';

describe('Service: lightingService', function () {

  // load the service's module
  beforeEach(module('fishWebApp'));

  // instantiate service
  var lightingService;
  beforeEach(inject(function (_lightingService_) {
    lightingService = _lightingService_;
  }));

  it('should do something', function () {
  });

});
